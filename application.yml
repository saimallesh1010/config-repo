global:
  message: Welcome to Microservices global configuration

logging:
  level:
    root: INFO

management:
  endpoints:
    web:
      exposure:
        include: health,info,refresh
  endpoint:
    health:
      show-details: always
